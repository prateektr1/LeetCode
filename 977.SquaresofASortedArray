class Solution {
    public int[] sortedSquares(int[] A) {
        
        int B[] = new int[A.length];
        int j=0;
        
        for(int i:A)
        {
            B[j]=i*i;
            j++;
        }
        
        Arrays.sort(B);
        
        return B;     
    }
}
